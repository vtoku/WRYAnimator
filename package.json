{
  "name": "wanim-to-fbx",
  "private": true,
  "version": "0.11.0",
  "type": "module",
  "description": "Browser-based Warudo .wanim → FBX animation converter. Runs entirely client-side.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "node --experimental-strip-types scripts/wanimSmoke.ts",
    "fbxcheck": "node scripts/fbxLoadCheck.mjs",
    "faceFbxCheck": "node scripts/faceFbxCheck.mjs",
    "drive": "node scripts/driveApp.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "playwright": "^1.60.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
